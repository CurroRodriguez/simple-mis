language: python
python:
    - "2.7"

branches:
    only:
        - master
        - development

install:
    - pip install -r requirements.txt

script: 
    - make -C docs html
    - nosetests ./tests/unit
    - behave ./tests/acceptance
